name: cbc-wine-menu
recipe: wordpress

config:
  webroot: wordpress
  php: '8.3'
  database: mariadb:10.11
  via: apache
  xdebug: false

tooling:
  wp:
    service: appserver
    cmd: wp --path=/app/wordpress
